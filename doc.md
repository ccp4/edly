experimental data frames should be in folder `exp` while simulated ones in `sim`
app.js requests /init
  init_session        # creates a session with default structure
  init_mol            # init strucutre info (cif file, exp data, etc...)
